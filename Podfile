# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'Marvel Characters' do
  use_frameworks!

  pod 'Alamofire', '~> 4.7'
  pod 'Moya', '~> 11.0'
  pod 'SwiftyJSON', '~> 4.0'

  target 'Marvel CharactersTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
